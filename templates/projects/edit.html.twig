{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}

<div class="block">

  <h2 class="center">Edit Project</h2>

    {{ form_start(form) }}

      <h2 class="tex">{{ form_label(form.title, 'Title', {'label_attr': {'class': ''}}) }}
      {{ form_widget(form.title, {'attr': {'class': 'text'}}) }}</h2>

      <button class="ttt">{{ button_label|default('Edit') }}</button>

    {{ form_end(form) }}

  <a href="{{ path('projects') }}">Back to projects</a>

</div>

{% endblock %}
